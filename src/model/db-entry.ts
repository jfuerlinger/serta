export class DbEntry {

    private _userId: number;
    private _levelId: number;

    constructor(
        userId: number,
        levelId: number) {
            this._userId = userId;
            this._levelId = levelId;
    }

}